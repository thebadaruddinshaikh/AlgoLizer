import 'package:flutter/material.dart';

class PathFinding extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text("Path Finding")),
      ),
    );
  }
}
